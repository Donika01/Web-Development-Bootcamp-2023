console.log("HELLO FROM OUR FIRST JS FILE!!!");
let total = 1 + 3;
console.log("GOODBYE!!!");
// We add the script at the bottom of the <body> because we want the html elements to be acknowledged by the browser before our script